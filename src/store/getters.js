export const canvas = state => state.canvas;

export const ctx = state => state.ctx;

export const imageUrl = state => state.imageUrl;

export const filename = state => state.filename;

export const filetype = state => state.filetype;

export const fileext = state => state.fileext;

export const image = state => state.image;

export const fillText = state => state.fillText;

export const colors = state => state.colors;

export const colorIndex = state => state.colorIndex;

export const opacity = state => state.opacity;

export const showAppName = state => state.showAppName;
